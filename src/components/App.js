import React from "react";
import Form from "./Form.js";
import { SafeAreaView } from "react-native";

const App = () => {
  return (
    <SafeAreaView>
      <Form />
    </SafeAreaView>
  );
};

export default App;
